export enum CommentsSortOrder {
  DEFAULT = 'default',
  NEWEST = 'newest',
  OLDEST = 'oldest',
  TOP_USER_KARMA = 'top_user_karma',
  LOW_USER_KARMA = 'low_user_karma',
  MOST_REPLIES = 'most_replies',
  LEAST_REPLIES = 'least_replies'
}